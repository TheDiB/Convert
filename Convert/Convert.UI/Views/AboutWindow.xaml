<Window x:Class="Convert.UI.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="A propos de"
        Width="300" Height="250"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="{Binding AppName, Mode=OneTime}" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="{Binding AppVersion, Mode=OneTime}" Margin="0 5 0 0"/>
            <TextBlock Text="{Binding AppDescription, Mode=OneTime}" TextWrapping="Wrap" Margin="0 10 0 0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0 5 0 0" HorizontalAlignment="Center">
            <Button Content="OK"
                    Margin="0 0 0 0"
                    Width="100"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
